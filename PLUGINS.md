# プラグインの作り方

Binder は Markdown のレンダリングに [marked](https://marked.js.org/) を使っており、
JS ファイルを置くだけで記法を拡張できる。このドキュメントはプラグインを書く人向けに、
**ファイルの形式・メタデータの宣言方法・バージョン互換の扱い**をまとめる。

> 現状 marked（Markdown）のみ対応。`plugins/mermaid/` は将来用の枠で未実装。

---

## 1. 置き場所

プラグインには2つの階層がある。

| 階層 | パス | 性質 |
|---|---|---|
| バインダー | `<バインダー>/plugins/marked/*.js` | **git 管理**。チーム全員に効く。実際に動くのはこちら |
| アプリ | `~/.binder/plugins/marked/*.js` | 全バインダー共通の**置き場**。ここに置くだけでは動かない |

アプリ階層は「配る元」で、バインダー設定の一覧から**インストール**して初めて動く。
そのため互換判定（現在の marked に対応しているか）はバインダー設定側にだけ表示される。

- ファイル名が識別子になる。使える文字は `[a-zA-Z0-9_-]`
- **読み込み順はファイル名のアルファベット順**。順序を制御したいときは
  `01-alerts.js` `02-footnotes.js` のようにプレフィックスを付ける
- 追加・更新・削除するとエンジンが再初期化され、次のプレビュー描画から反映される

追加はバインダー設定 → プラグインタブから JS ファイルを選ぶ。

---

## 2. ファイルの形式

**IIFE で `marked.use()` 互換のオブジェクトを返す**。ファイル全体が `eval` され、
戻り値がそのまま `marked.use()` に渡る。

```js
/* @plugin-name: Keyboard Tag ([[Key]]) */
/* @plugin-version: 1.0.0 */
/* @marked: >=14 <19 */
(function() {
  return {
    extensions: [
      {
        name: 'kbd',
        level: 'inline',
        start: function(src) {
          var m = src.match(/\[\[/);
          return m ? m.index : undefined;
        },
        tokenizer: function(src) {
          var m = src.match(/^\[\[([^\]]+)\]\]/);
          if (!m) return;
          var token = { type: 'kbd', raw: m[0], tokens: [] };
          // 中身をインライントークンとして解析しておく（後述の原則1）
          this.lexer.inline(m[1], token.tokens);
          return token;
        },
        renderer: function(token) {
          return '<kbd>' + this.parser.parseInline(token.tokens) + '</kbd>';
        }
      }
    ],
    // コア renderer の上書き・hooks・walkTokens も使える
    // renderer: { blockquote: function(token) { ... } },
    // hooks: { preprocess: function(md) { ... } },
    // walkTokens: function(token) { ... },
  };
})();
```

オブジェクト以外を返した場合・例外を投げた場合は適用されず、設定画面に
「読み込み失敗」として表示される。

---

## 3. メタデータの宣言

**ファイル先頭のコメント行**で宣言する。3つとも省略できるが、省略した場合の扱いは
後述のとおり不利になるので、**3つとも書くことを推奨する**。

```js
/* @plugin-name: Keyboard Tag ([[Key]]) */
/* @plugin-version: 1.0.0 */
/* @marked: >=14 <19 */
```

- `/* @key: value */` 形式のほか `// @key: value` 形式も使える
- 走査するのは**先頭40行まで**。それ以降に書いても読まれない
  （本文中の `@marked` 等を誤検出しないため）
- 同じキーが複数あった場合は**最初のものだけ**が使われる

### `@plugin-name` — 表示名

設定画面の一覧に出る名前。省略した場合はファイル名が使われる。

記法が分かる形（例: `Keyboard Tag ([[Key]])`）にしておくと、一覧を見ただけで
何のプラグインか分かる。

### `@plugin-version` — プラグイン自身のバージョン

`x.y.z` 形式のセマンティックバージョン。設定画面の一覧に `v1.0.0` と表示される。
省略すると `-` になる。

Binder はこの値を**表示するだけ**で、動作を変えたり更新判定に使ったりはしない。
配布したプラグインを受け取った人が「自分の持っているものが新しいか」を
判断するための情報。付け方の目安は次のとおり。

| 変更内容 | 上げ方 |
|---|---|
| 出力される HTML が変わる・記法が変わる | マイナー（`1.0.0` → `1.1.0`） |
| 挙動を変えない修正・コメントやメタデータだけの変更 | パッチ（`1.0.0` → `1.0.1`） |
| 既存の書き方が動かなくなる変更 | メジャー（`1.0.0` → `2.0.0`） |

### `@marked` — 対応する marked のバージョン範囲

**このプラグインが動作を確認できている marked の範囲**を宣言する。
範囲を満たさない marked では**適用されない**（スキップされ、理由が警告に出る）。

```js
/* @marked: >=14 <19 */   // 14 以上 19 未満
/* @marked: >=17 */       // 17 以上
/* @marked: 18 */         // 18 系のみ（演算子なしの整数はメジャー一致）
```

構文は限定的で、以下だけをサポートする。

| 使えるもの | 意味 |
|---|---|
| `>=` `>` `<=` `<` `=` | 比較 |
| 空白区切り | **AND**（すべて満たす必要がある）。OR は書けない |
| 演算子なしの整数（例: `14`） | そのメジャーバージョンに一致 |

**`^` と `~` は使えない**（解釈が揺れるため意図的に非対応）。`^1.2.3` のつもりで
書いても正しく判定されないので、`>=1.2.3 <2` のように展開して書くこと。

比較は常に `major.minor.patch` の3要素で行う。`>=14` は `14.0.0` として扱われる。

#### 何を書けばいいか

`@marked` は**作者の自己申告**であり、動作の証明ではない。実際にテストした範囲を
書くのが原則。判断に迷う場合は次を目安にする。

- 上限は「まだ存在しないメジャー」で切る（例: 現行が 18 系なら `<19`）。
  上限を書かないと、将来の破壊的変更をそのまま踏むことになる
- コア renderer（`heading` / `codespan` 等）を上書きしている場合は `>=15` 以上に絞るか、
  後述の原則4を守ったうえで広く取る
- トークン構造に依存している場合は狭めに宣言する（marked は minor でも
  トークン構造を変えることがある）

#### 宣言しなかった場合

`@marked` を書かなくても適用はされる（後方互換のため）。ただし Binder は
「そのプラグインを追加・更新したときに動いていた marked のメジャー」をローカルに
記録しており、それと現在の marked が食い違うと警告が出る。

| 状況 | 挙動 | 設定画面 |
|---|---|---|
| 宣言あり・範囲を満たす | 適用 | 緑 |
| 宣言あり・範囲を満たさない | **適用しない** | 赤（非対応） |
| 宣言あり・marked のバージョンが不明 | 適用（警告） | 灰 |
| 未宣言・記録と一致 | 適用 | 緑 |
| 未宣言・記録と不一致 | 適用（「動作未確認」の警告） | 橙 |
| 未宣言・記録なし | 適用 | 灰 |

この記録は `~/.binder/setting.json` にバインダーごとに保存されるローカルな観測値で、
git では共有されない。**配布するプラグインは必ず `@marked` を宣言すること。**

---

## 4. 実行環境（`globalThis.binder`）

プラグインの評価前に、Binder が次のオブジェクトを用意する。参照は任意で、
使わないプラグインは何の影響も受けない。

```js
globalThis.binder = {
  marked: {
    version: "17.0.5",   // 判明している場合のみ。不明なら null
    major: 17,           // ほぼ常に取れる
    source: "vendor",    // "vendor"（内蔵） | "cdn"
    satisfies(range) { ... },   // @marked と同じ構文で真偽を返す
  },
  escape(str) { ... },   // HTML エスケープ（& < > " ' を変換）
};
```

marked 本体はバージョンを公開していない（`marked.version` は v14〜v18 すべて
`undefined`）ため、バージョンを知るにはこれを使う。

```js
renderer: function(token) {
  var b = globalThis.binder;
  if (b && b.marked.satisfies('>=17')) {
    // v17 以降だけの書き方
  }
  return '...';
}
```

**Binder 外（テスト等）では `globalThis.binder` が存在しない**ことがあるので、
参照する場合は必ず存在チェックを入れること。

---

## 5. 書き方の原則

同梱プラグインが marked v14 から v18 まで無傷だったのは、次を守っていたため。

### 1. トークンの中身は parser に委譲する

```js
// 推奨: エスケープ仕様の変更に影響されない
renderer: function(token) {
  return '<div>' + this.parser.parse(token.tokens) + '</div>';        // ブロック
}
renderer: function(token) {
  return '<span>' + this.parser.parseInline(token.tokens) + '</span>'; // インライン
}

// 避ける: カスタム拡張のトークンは marked がエスケープしない
renderer: function(token) { return '<kbd>' + token.text + '</kbd>'; }
```

`token.text` を直接埋めると、中身の `&` や `<` がそのまま出力される（不正な HTML、
場合によっては XSS）。parser に通せばエスケープも強調記法も正しく処理される。

### 2. 直接埋めるなら自分でエスケープする

どうしても文字列を埋める場合は `binder.escape()` を使う。
**属性値に埋めるときは `"` のエスケープが必須**。

### 3. トークン構造に依存しない

`item.tokens[0].type === 'text'` のような判定は marked のバージョンで壊れる
（v17 でタスクリストの子トークンに `checkbox` が追加された等）。`token.raw` の
内容にも依存しないこと（v18 で末尾空行の扱いが変わっている）。

### 4. コア renderer を上書きする場合はエスケープ前提が変わることに注意する

v15 以降、コア renderer に渡る `token.text` は**エスケープ前**になった。

```
codespan の token.text
  v14 : "a &amp; &lt;b&gt;"   （エスケープ済み）
  v15〜: "a & <b>"            （生）
```

v14 時代の書き方のまま v15 以降で動かすと、生の HTML を出力してしまう。

### 5. `hooks.preprocess` は慎重に

`preprocess` は本文全体を書き換えるため、**コードブロックや生 HTML を巻き込みやすい**。
特に生 HTML のタグを書き換えると、属性値が壊れて描画が崩れるだけでなく、
HTML パーサが属性を大量に生成して極端に遅くなることがある。

書き換えたくない箇所（コードブロック・インラインコード・HTML コメント・タグ）は
いったん退避してから処理すること。実装例は同梱の `smartypants.js` を参照。

可能なら preprocess ではなく、トークン単位（`walkTokens` や renderer）で処理するほうが安全。

### 6. `@marked` を宣言する

前述のとおり。

---

## 6. 動作の確認

### 設定画面

バインダー設定 → プラグインタブに、プラグインごとの状態がドットの色で出る。
行にカーソルを合わせると詳細（状態 / 対応marked / 現在のmarked）が表示される。

| 色 | 意味 |
|---|---|
| 緑 | 適用されている |
| 赤 | 非対応でスキップ / 読み込み失敗 / 実行時エラー / 未適用 |
| 橙 | 動作未確認（未宣言で、記録した marked と食い違う） |
| 灰 | 未宣言・判定不能 |

一覧の上に、いま動作している marked のバージョンと取得元（内蔵 / CDN）が出る。

### プレビュー・出力

プラグインに問題があると、プレビューの警告バーに件数が出る。出力（公開）時にも
成功ではなく警告が表示される。**出力した HTML は git に記録されるため、
劣化したまま記録していないか確認すること。**

### エラーの隔離

プラグインが例外を投げても、プレビュー全体は落ちない。

- `tokenizer` が投げた → その記法はマッチせず、素のテキストが残る
- `renderer` が投げた → 元のソース（エスケープ済み）が出力される
- いずれも設定画面と警告に「実行時エラー」として記録される

**静かに消えることはない**が、気付くには警告を見る必要がある。

### marked のバージョンを変えて試す

バインダー設定の「スクリプト」タブで marked の CDN URL を指定すると、その版で動作させられる。
指定できるのは許可された CDN のみ（既定は jsdelivr / cdnjs / unpkg）。

```
https://cdn.jsdelivr.net/npm/marked@18.0.7/lib/marked.esm.js
```

**必ず `.esm.js` を指定すること**（`lib/marked.umd.js` は読み込めない）。
この設定は `binder.json` に入るため git 管理され、チーム全員に効く。

同梱の `marked-info.js` プラグインを有効にして、ノートに `@info` と書くと、
動作中の marked のバージョン・読み込まれている拡張・オプションを確認できる。

---

## 7. テスト

配布するプラグインは、対象の marked に対して実際に描画させるテストを書いておくと安全。
marked のバージョン差による破壊は**例外を投げず、静かに出力だけが変わる**ため、
テスト以外では検出できない。

```js
import { Marked } from 'marked';   // 対象バージョンを npm i marked@17 等で入れる
import fs from 'node:fs';

const plugin = (0, eval)(fs.readFileSync('kbd.js', 'utf8'));

const inst = new Marked();
inst.use(plugin);

test('特殊文字がエスケープされる', () => {
  expect(inst.parse('[[Ctrl & C]]')).toContain('&amp;');
});
```

同梱プラグインのテストは `_cmd/binder/frontend/src/__tests__/bundledPlugins.test.jsx`
にあり、バンドルしている marked の実体に対して全本を適用している。

---

## 8. 同梱プラグインを修正する場合（Binder 本体への貢献）

`setup/_assets/plugins/marked/` のファイルを更新したときは、以下も合わせて行う。

1. `@plugin-version` を上げる
2. **更新前のファイルの sha256 を `setup/plugin_sync.go` の `shippedPluginHashes` に追記する**
   （改行を LF に正規化した内容のハッシュ）

2 を忘れると、その版を持っているユーザのファイルが「ユーザが編集したもの」と
判定され、以降の更新が永久に届かなくなる。

ユーザ環境への配布はアプリのバージョンが変わったときに行われる。ユーザが編集した
ファイル・意図的に削除したファイルは上書きしない。
